package com.ner.landslide.entity.enums;

/** Role-based access control roles used across the platform. */
public enum Role {
    CITIZEN,
    FIELD_OFFICER,
    DISTRICT_ADMIN,
    DISASTER_ADMIN,
    SUPER_ADMIN
}
